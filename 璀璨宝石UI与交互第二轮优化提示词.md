请继续修改当前《璀璨宝石 Splendor》网页游戏。

本轮**不要重新设计整个界面**，保留目前已经完成的圆桌化布局和游戏规则，重点解决下面三个实际使用问题：

# 1. 游戏记录无限向下增长，导致整个页面越来越长
# 2. 电脑玩家区域需要显示“上一回合具体做了什么”
# 3. 发展卡被购买/预留后，新牌必须原位置补位，不能整排左移

请直接修改当前 HTML / CSS / JavaScript，并实际测试。

---

# 一、修复游戏记录导致整个页面无限变长的问题

## 当前问题

右侧：

> 游戏记录

现在每进行一个行动，就继续往下面新增一行。

结果是：

```text
回合越来越多
↓
日志越来越长
↓
右侧栏越来越长
↓
中央桌面被一起撑高
↓
整个网页变成非常非常长的一页
```

这必须修改。

---

# 二、游戏记录必须变成“固定高度滚动窗口”

游戏日志区域必须具有：

> 固定高度 / 最大高度

不能因为日志数量增加而继续撑高整个页面。

例如：

```css
.game-log {
    height: 300px;
    max-height: 300px;
    overflow-y: auto;
}
```

具体高度请根据当前UI空间自行调整，不要求机械使用300px。

核心要求：

> 无论进行20回合、50回合还是100回合，游戏记录区域的外部尺寸都不能继续增长。

新增日志只能在这个固定框内部滚动。

---

# 三、新日志自动滚动到底部

每产生一条新记录后：

> 游戏日志窗口自动滚动到最新一条。

例如：

```text
第31回合……
第32回合……
第33回合……
↓
自动显示最新记录
```

玩家不需要每回合手动拖滚动条。

但如果实现方便，可以保留用户手动查看旧记录的能力。

---

# 四、不要让日志决定整个页面高度

这一点非常重要。

请检查当前布局中是否存在：

- grid item 被日志内容撑高；
- flex item 因内容自动扩张；
- 父容器使用 min-height:auto；
- 右侧栏高度反向影响中央桌面高度；
- game-log没有overflow限制。

请从布局层面解决。

最终要求：

> 游戏主界面整体高度应主要由桌面区域决定，而不是由日志长度决定。

正常桌面浏览器中，最好能够做到：

> 一屏或接近一屏完成主要游戏操作。

可以有少量必要滚动，但绝不能因为游戏进行到几十回合而产生几屏甚至十几屏高度。

---

# 五、游戏日志本身仍然保留完整历史

不要简单通过：

```text
只保存最近10条
```

来逃避问题。

逻辑层面仍然可以保存完整：

```javascript
gameState.logs
```

例如100条。

UI只是在：

> 固定高度容器内滚动显示。

如果为了性能需要，也可以只渲染最近一定数量，例如100～200条，但不能影响正常一局游戏的完整记录。

---

# 六、电脑玩家区域增加“上一回合行动”

当前电脑1、电脑2的玩家面板已经显示：

- 分数
- 当前筹码
- 永久宝石
- 发展卡数量
- 贵族
- 预留数量
- 难度

现在请增加一个非常重要的信息：

# 上回合行动

放在电脑1和电脑2自己的信息区域里。

目的是：

> 玩家不需要一直盯着游戏日志，也能马上知道刚刚电脑做了什么。

---

# 七、AI如果上一回合拿宝石

例如电脑1上一回合拿了：

```text
白 ×1
蓝 ×1
黑 ×1
```

电脑1区域显示：

```text
上回合：
拿取 💎白×1 🔵蓝×1 ⚫黑×1
```

如果拿两枚同色：

```text
上回合：
拿取 🔴红×2
```

尽量用：

- 当前游戏已有的宝石圆点；
- 对应颜色；
- 简短文字；

不要只写：

> 拿了3个宝石

必须精确显示：

> 哪些颜色、各多少枚。

---

# 八、AI如果上一回合购买发展卡

这是本轮重点。

不能只写：

```text
购买了一张第2层发展卡
```

必须让玩家知道：

> 到底是哪一张。

需要至少显示以下三个信息：

## 1. 这张卡提供的永久宝石颜色

例如：

```text
奖励：红宝石
```

或者使用红色圆点/红宝石图标。

---

## 2. 这张卡的分值

例如：

```text
2分
```

---

## 3. 这张卡的原始购买成本

例如：

```text
花费：
白3 + 黑5
```

注意：

这里显示的是：

# 卡牌印刷的原始成本

不是AI因为永久折扣以后实际支付了多少钱。

例如卡牌原始成本：

```text
白3 黑5
```

AI因为永久宝石折扣实际只支付：

```text
白1 黑3
```

电脑区域仍然应该优先显示：

```text
购买：
2分 · 红宝石卡
成本：白3 黑5
```

因为我要识别：

> 它到底买走了桌面的哪张发展卡。

---

# 九、推荐的AI上回合卡牌展示格式

不要塞成很长的一句话。

可以做成一个小型信息块：

```text
上回合
购买发展卡

🔴 红宝石
2 分

成本：
⚪3  ⚫5
```

或者横向紧凑显示：

```text
上回合：购买 🔴红宝石卡｜2分｜⚪3 ⚫5
```

根据当前电脑面板空间自行决定。

原则：

> 信息完整，但不能把电脑面板撑得特别高。

---

# 十、AI预留卡也必须精确显示

如果电脑上一回合：

> 预留了一张公开发展卡

也要显示：

- 等级；
- 永久宝石颜色；
- 分值；
- 原始成本。

例如：

```text
上回合：
预留 第3层
⚪白宝石卡 · 4分
成本：🔴7
获得黄金×1
```

如果是：

> 盲抽预留

因为玩家正常规则下不知道盲抽卡的具体内容，则不能泄露隐藏信息。

应该只显示：

```text
上回合：
盲抽预留 第2层
获得黄金×1
```

**不要显示电脑盲抽预留卡的颜色、分值和成本。**

这是重要的隐藏信息规则。

---

# 十一、AI获得贵族时也显示

如果电脑购买发展卡后获得贵族，可以在上一回合信息中追加：

```text
并获得贵族 +3分
```

如果空间允许，也可以显示贵族需求。

例如：

```text
获得贵族：红4 + 黑4
```

---

# 十二、“上一回合行动”必须是独立状态数据

不要通过：

> 从游戏日志最后一行临时解析文字

来实现。

请在玩家/AI状态里保存结构化信息，例如：

```javascript
lastAction: {
    type: "takeTokens",
    tokens: {
        white: 1,
        blue: 1,
        black: 1
    }
}
```

购买发展卡时：

```javascript
lastAction: {
    type: "buyCard",
    card: {
        level: 2,
        bonus: "red",
        points: 2,
        cost: {
            white: 3,
            black: 5
        }
    },
    gainedNoble: null
}
```

预留公开卡时：

```javascript
lastAction: {
    type: "reserveCard",
    hidden: false,
    card: {...},
    gainedGold: true
}
```

盲抽时：

```javascript
lastAction: {
    type: "reserveCard",
    hidden: true,
    level: 3,
    gainedGold: true
}
```

这样以后UI修改更方便。

---

# 十三、当前正在行动时不要马上覆盖“上一回合”

例如：

```text
电脑1：
上回合：购买2分红宝石卡
当前：等待
```

轮到电脑1再次行动时，可以显示：

```text
上回合：购买2分红宝石卡
当前：思考中……
```

等电脑1真正完成新行动以后：

> 再把lastAction替换为刚完成的新行动。

---

# 十四、发展卡补牌逻辑必须改成“原卡位补牌”

当前存在一个明显的视觉问题。

假设某一层公开牌当前是：

```text
位置1    位置2    位置3    位置4
A        B        C        D
```

如果玩家购买：

```text
位置2的B
```

现在的效果可能变成：

```text
A        C        D        新牌E
```

也就是：

> 后面所有牌向左移动，新牌从最右边补进来。

这个行为需要修改。

---

# 十五、正确的补牌方式

如果拿走：

```text
位置2的B
```

那么应该：

```text
位置1    位置2    位置3    位置4
A        新牌E    C        D
```

也就是说：

> 新牌必须直接补到刚刚空出来的那个位置。

其他三张牌：

> 完全不能移动。

---

# 十六、原因

实体《璀璨宝石》桌游中，桌面上的四个卡位在视觉上是稳定的。

数字版也应该保持：

# Card Slot = 固定卡位

否则玩家刚刚记住：

```text
右边第二张牌
```

有人购买另一张以后：

> 所有卡突然位移

会增加视觉负担。

因此必须改成：

> “哪一格被拿走，就在哪一格补新牌。”

---

# 十七、数据层也不要使用会导致整体shift的处理方式

请检查当前代码是不是类似：

```javascript
faceUpCards.splice(index, 1);
faceUpCards.push(drawCard());
```

这种写法天然会导致：

```text
后面的牌整体左移
```

请改成固定索引替换，例如：

```javascript
faceUpCards[index] = drawCard();
```

如果牌堆已经为空：

```javascript
faceUpCards[index] = null;
```

UI则保留该槽位为空。

---

# 十八、四个卡位必须长期固定

每层：

```text
第3层
Slot 0
Slot 1
Slot 2
Slot 3
```

```text
第2层
Slot 0
Slot 1
Slot 2
Slot 3
```

```text
第1层
Slot 0
Slot 1
Slot 2
Slot 3
```

这些槽位必须固定。

购买、公开预留以后：

> 只替换对应slot。

不能重新排列整个face-up数组。

---

# 十九、预留公开卡同样遵守原位补牌

不仅购买。

如果玩家/AI：

> 预留桌面上的某一张公开卡

也应该：

```text
拿走哪一个位置
↓
新牌就在这个位置翻开
```

不能让其它三张牌移动。

---

# 二十、盲抽预留不影响公开牌

如果玩家：

> 从牌堆盲抽预留

那么只从牌堆顶部减少一张。

当前4张公开牌：

> 全部保持原位置不动。

---

# 二十一、卡牌DOM也尽量保持稳定

如果当前实现每次补牌都重新创建整行DOM，可以考虑改成：

```text
每层固定4个card-slot
```

例如：

```html
<div class="card-slot" data-slot="0"></div>
<div class="card-slot" data-slot="1"></div>
<div class="card-slot" data-slot="2"></div>
<div class="card-slot" data-slot="3"></div>
```

更新某张牌时：

> 只更新对应slot内容。

这样以后增加：

- 翻牌动画；
- 新牌淡入；
- 被购买牌淡出；

都会更容易。

---

# 二十二、可以增加非常轻微的补牌动画

如果实现成本低，可以加入：

```text
旧牌被拿走
↓
原位置短暂空出
↓
新牌淡入 / 翻牌出现
```

持续：

```text
150～300ms
```

即可。

不要出现：

> 整排卡左右滑动。

新牌的视觉变化应该发生在：

# 原来的位置。

---

# 二十三、顺便优化当前页面高度

结合游戏记录固定高度以后，请再检查当前圆桌UI。

目前页面下方存在：

> 中央桌面容器被拉得非常高，但下半部分大量空白。

请检查：

- `height`
- `min-height`
- `100vh`
- grid row
- flex stretch
- 日志高度
- 玩家区定位

避免出现：

```text
桌面内容只有上半部分
↓
中间下面大片纯色空白
↓
玩家区很远才出现
```

目标应该是：

> 电脑座位 + 贵族 + 三层发展卡 + 玩家主座位，在一屏内形成紧凑的围桌结构。

在1920×1080级别桌面屏幕上，应优先争取：

> **主要游戏区域基本一屏可见。**

不要求完全零滚动，但不能出现现在这种巨大的无意义纵向空白。

---

# 二十四、本轮不要改动的内容

这次不要重新修改：

- 卡池数据；
- 贵族数据；
- AI难度算法；
- 宝石规则；
- 购买规则；
- 胜负规则；
- 当前整体紫金视觉风格。

重点只解决：

```text
1. 游戏日志固定高度
2. AI上回合行动精确展示
3. 发展卡固定卡位补牌
4. 顺便消除由布局造成的大面积纵向空白
```

---

# 二十五、验收测试

完成以后必须自行测试以下情况。

## 测试1：长局日志

模拟至少：

```text
100个行动
```

确认：

- 游戏日志内部滚动；
- 整个网页高度不会因为日志持续增加；
- 中央桌面不会被日志撑长。

---

## 测试2：AI拿3种宝石

确认AI区域显示：

```text
上回合：
白×1 蓝×1 黑×1
```

颜色和数量正确。

---

## 测试3：AI拿2枚同色

确认显示：

```text
红×2
```

---

## 测试4：AI购买发展卡

确认AI区域准确显示：

- 卡牌永久奖励颜色；
- 卡牌分值；
- 卡牌原始成本。

必须能通过这些信息唯一识别AI刚刚拿走的是哪张桌面发展卡。

---

## 测试5：AI公开预留

显示：

- 等级；
- 颜色；
- 分值；
- 成本；
- 是否获得黄金。

---

## 测试6：AI盲抽预留

只显示：

- 哪一层盲抽；
- 是否获得黄金。

禁止泄露隐藏卡牌内容。

---

## 测试7：固定卡位

例如第2层：

```text
A B C D
```

购买B以后必须变成：

```text
A E C D
```

而不能：

```text
A C D E
```

---

## 测试8：公开预留固定卡位

预留C以后：

```text
A B E D
```

其它牌不得移动。

---

## 测试9：牌堆耗尽

如果被拿走的位置已经没有新牌可补：

> 对应slot保持为空。

其它公开牌依然不得移动。

---

# 二十六、最终目标

完成以后，玩家应该获得三个非常明确的体验改善：

### 第一
游戏无论玩多少回合：

> 整个页面都不会因为游戏日志越来越长而无限向下增长。

### 第二
电脑完成行动后：

> 我只看电脑的座位区，就可以立即知道它上一回合拿了什么宝石，或者具体购买/预留了哪张牌。

### 第三
桌面发展卡具有稳定位置记忆：

> 哪张牌被拿走，新牌就在哪里补出来，其他牌完全不动。

请直接读取当前项目代码并开始修改，不要只告诉我应该怎么实现。

完成后自行进行长局、AI行动展示和固定卡位补牌的回归测试。